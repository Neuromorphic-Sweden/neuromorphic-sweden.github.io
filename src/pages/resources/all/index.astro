---
import { getCollection } from 'astro:content';
import DiscoveryCard from '../../../components/DiscoveryCard.astro';
import SiteLayout from '../../../layouts/SiteLayout.astro';
import reviewQueue from '../../../data/discovery-review-queue.json';
import { sortDiscoveries } from '../../../lib/discovery';

const reviewSite = process.env.REVIEW_SITE === 'true';
const reviewCandidateIds = new Set(reviewQueue.candidateIds);
const entries = sortDiscoveries(
	await getCollection(
		'discoveries',
		({ data }) => !data.internal,
	),
);
---

<SiteLayout
	title="All discovered content"
	description="Published AI-assisted records, ordered by the date of the original source, event or identification."
>
	<section class="page-hero">
		<div class="container">
			<a class="back-link" href="/resources/"><span aria-hidden="true">←</span> Back to Resources</a>
			<p class="eyebrow">AI-assisted discovery</p>
			<h1>All discovered content.</h1>
			<p class="lead">
				Published records are listed newest first. Dates represent an event date or the
				original source's publication date when available; otherwise, they show when the
				information was identified by Neuromorphic Sweden. Event schedules and registration
				details should be confirmed with the original organiser.
			</p>
		</div>
	</section>

	<section class="section">
		<div class="container">
			<p class="archive-count">{entries.length} {reviewSite ? 'records in this build' : `published ${entries.length === 1 ? 'record' : 'records'}`}</p>
			{entries.length > 0 ? (
				<div class="archive-grid">
					{entries.map((entry) => <DiscoveryCard entry={entry} review={reviewSite && reviewCandidateIds.has(entry.data.candidateId)} />)}
				</div>
			) : (
				<div class="empty-state">
					<h2>No published records yet.</h2>
					<p>Selected records will appear here after publication.</p>
				</div>
			)}
		</div>
	</section>
</SiteLayout>

<style>
	.back-link {
		display: block;
		width: fit-content;
		margin-bottom: 2rem;
		font-weight: 750;
		text-decoration: none;
	}

	.archive-count {
		margin-bottom: 1.5rem;
		color: var(--blue-700);
		font-size: 0.76rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.archive-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
	}

	.empty-state {
		border: 1px solid var(--line);
		border-radius: 1rem;
		padding: clamp(1.5rem, 4vw, 3rem);
	}

	.empty-state h2 {
		font-size: clamp(1.6rem, 3vw, 2.2rem);
	}

	@media (max-width: 64rem) {
		.archive-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}

	@media (max-width: 42rem) {
		.archive-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
